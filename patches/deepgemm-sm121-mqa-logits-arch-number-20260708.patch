diff --git a/csrc/jit/device_runtime.hpp b/csrc/jit/device_runtime.hpp
index d433558..e5912d7 100644
--- a/csrc/jit/device_runtime.hpp
+++ b/csrc/jit/device_runtime.hpp
@@ -103,7 +103,7 @@ public:
             return support_arch_family ? "100f" : "100a";
         }
         // SM12.1 reuses the SM120 family cubin when `sm_<NN>f` is available.
-        if (major == 12 and (minor != 1 or support_arch_family)) {
+        if (major == 12 and (minor != 1 or support_arch_family or number_only)) {
             if (number_only)
                 return "120";
             return support_arch_family ? "120f" : "120a";
